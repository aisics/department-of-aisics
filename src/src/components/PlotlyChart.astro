---
export interface Props {
  id: string;
  className?: string;
}

const { id, className = '' } = Astro.props;
---

<div id={id} class={`plotly-chart ${className}`}></div>

<style>
  .plotly-chart {
    width: 100%;
    height: 400px;
    margin: 1rem 0;
  }
</style>